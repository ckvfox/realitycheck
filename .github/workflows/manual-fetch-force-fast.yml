name: 🚀 RealityCheck – Manual Force+Fast Fetch (Clean+NoAnalysis)

on:
  workflow_dispatch: {}    # Nur manuell startbar

jobs:
  manual_force_fast_run:
    runs-on: ubuntu-latest
    env:
      TZ: Europe/Berlin
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

    steps:
      - name: 🧭 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas tqdm python-dotenv openai
          echo "✅ Dependencies installed"

      - name: 🚀 Run RealityCheck FORCE+FAST fetch (clean slate, no analysis)
        run: |
          echo "===== RealityCheck Manual FORCE+FAST Fetch Start $(date -u) ====="
          python ./scripts/fetch_data.py -f -n
          echo "===== Manual Force+Fast Fetch End $(date -u) ====="

      - name: 💾 Upload log artifact
        uses: actions/upload-artifact@v4
        with:
          name: fetch_log_manual_force_fast
          path: data/fetch_log.txt
          retention-days: 7

      - name: 🌍 FTP Deploy /data to InfinityFree (Safe)
        uses: SamKirkland/ftp-deploy-action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps
          local-dir: data/
          server-dir: ${{ secrets.FTP_DIR }}/data/
          state-name: .ftp-deploy-sync-state-data-force-fast.json
          log-level: verbose
          dangerous-clean-slate: false       # 🚫 Kein Löschen!
          exclude: |
            **/.git*
            **/.github*
            **/__pycache__/
            **/*.py
            **/*.yml
            **/*.csv
            **/pending/**

      - name: 🪶 Commit and push updated data to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "RealityCheck Bot"
          git config --global user.email "bot@realitycheck"
          git add data/
          git commit -m "🚀 Manual FORCE+FAST data update ($(date -u))" || echo "No changes to commit"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }} HEAD:main

      - name: 🕐 Get current UTC date
        id: date
        run: echo "date=$(date -u '+%a, %d %b %Y %H:%M:%S UTC')" >> $GITHUB_OUTPUT

      - name: 📱 Create GitHub Issue for status
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const title = `🚀 Manual FORCE+FAST Fetch – ${{ job.status }}`;
            const body = `
            🧭 **Manual RealityCheck FORCE+FAST fetch completed**
            
            - **Status**: ${{ job.status }}
            - **Date**: ${{ steps.date.outputs.date }}
            - **Mode**: FORCE+FAST (clean slate + no GPT analysis)
            - **Run**: [View full log](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
            
            ⚡ **Maximum speed**: Fresh data fetch without time-consuming analysis generation.
            
            ---
            *This issue was created automatically by the RealityCheck Bot.*
            `;
            
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: title,
              body: body,
              labels: ['automation', 'manual-run', 'force-fetch', 'fast-fetch']
            });